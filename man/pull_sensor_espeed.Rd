% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pull_sensor_espeed.R
\name{pull_sensor_espeed}
\alias{pull_sensor_espeed}
\title{Pull sensor estimated speed data}
\usage{
pull_sensor_espeed(
  sensor,
  pull_date,
  fill_gaps = TRUE,
  length_ft_min = NULL,
  length_ft_max = NULL,
  headway_sec_min = NULL,
  headway_sec_max = NULL,
  district = "metro",
  .quiet = TRUE
)
}
\arguments{
\item{sensor}{character, the sensor ID.
See \code{\link[=pull_sensor_ids]{pull_sensor_ids()}} to obtain metro sensor IDs.}

\item{pull_date}{character, the date of data to pull.
Accepts either \code{"YYYY-MM-DD"} or \code{"YYYYMMDD"} format.}

\item{fill_gaps}{logical, whether to fill gaps in the time series with \code{NA}
values. Default is \code{TRUE}}

\item{length_ft_min}{numeric, minimum vehicle length in feet for filtering. Optional. Default is \code{NULL.}}

\item{length_ft_max}{numeric, maximum vehicle length in feet for filtering (non-inclusive). Optional. Default is \code{NULL.}}

\item{headway_sec_min}{numeric, minimum headway in seconds for filtering. Optional. Default is \code{NULL.}}

\item{headway_sec_max}{numeric, maximum headway in seconds for filtering (non-inclusive). Optional. Default is \code{NULL.}}

\item{district}{character, MnDOT district code. Default is "metro".
Use \code{\link[=mayfly_get_districts]{mayfly_get_districts()}} to see available districts.}

\item{.quiet}{logical, whether to suppress error messages. Default TRUE}
}
\value{
data.table containing variables espeed, sensor, date, hour, min.
}
\description{
Fetch estimated speed data for a single date and sensor from the
Mayfly API. Use \code{\link[=pull_sensor_ids]{pull_sensor_ids()}} to obtain metro sensor IDs.
Estimated speed is calculated following methods described \href{https://github.com/mnit-rtmc/iris/blob/master/mayfly/ESPEED.md}{here}.
}
\details{
\subsection{Estimated vs Measured Speed}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{Estimated speed is calculated using free-flow speed and traffic conditions.
Use [pull_sensor_speed()] for measured speed data.
}\if{html}{\out{</div>}}
}

\subsection{Filtering}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{The filtering parameters allow filtering of vehicle observations based on
estimated speed, vehicle length, and headway. This can be useful for traffic
analysis or identifying specific traffic patterns.
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
# Simple example
espeed_data <- pull_sensor_espeed(5474, "2018-10-14")

# With filtering - passenger cars at typical speeds
filtered_data <- pull_sensor_espeed(5474, "2018-10-14",
  length_ft_min = 10,
  length_ft_max = 20,
  speed_mph_min = 30,
  speed_mph_max = 70
)
}

}
\seealso{
Other loop sensor functions: 
\code{\link{pull_configuration}()},
\code{\link{pull_sensor}()},
\code{\link{pull_sensor_headway}()},
\code{\link{pull_sensor_ids}()},
\code{\link{pull_sensor_length}()},
\code{\link{pull_sensor_speed}()}
}
\concept{loop sensor functions}
